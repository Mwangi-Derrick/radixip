[package]
name = "radixip-policy-ffi"
version = "0.1.0"
edition = "2021"
description = "C ABI for RadixIP configuration and policy decisions"
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
radixip_rs = { path = "../engine", version = "1.1.1" }
radixip-config = { path = "../config", version = "0.1.0" }
radixip-policy = { path = "../policy", version = "0.1.0" }
tokio = { workspace = true }
