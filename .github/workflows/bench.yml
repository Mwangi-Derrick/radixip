name: Continuous Benchmarking

# 1. Comment out or delete your push/pull request triggers
# on:
#   push:
#     branches: [ main ]
      # paths-ignore:
      # - '**.md'
      # - 'docs/**' # Optional: Ignores an entire docs folder
      # - 'proto'
      #   - 'docker'
      #   - '.gitignore'
      #   - '.gitattributes'
      #   - 'scripts/**'
      #   - 'config'
      #   - 'metrics'
      #   - '.editorconfig'
      #   - 'Makefile'
#   pull_request:
#     branches: [ main ]
      # paths-ignore:
      # - '**.md'
      # - 'docs/**' # Optional: Ignores an entire docs folder
      # - 'proto'
    #   - 'docker'
    #   - '.gitignore'
    #   - '.gitattributes'
    #   - 'scripts/**'
    #   - 'config'
    #   - 'metrics'
    #   - '.editorconfig'
    #   - 'Makefile'

# 2. Add this instead to make it strictly manual
on:
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: Generate dataset
        run: python scripts/generate_mock_data.py
        
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'
          
      - name: Run Go benchmarks
        run: |
          cd radixip-go
          go test -bench=. -benchmem ./... > ../go_bench_results.txt
          
      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        
      - name: Run Rust benchmarks
        run: |
          cd radixip_rs
          cargo bench -- --output-format bencher > ../rust_bench_results.txt
          
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: performance-metrics
          path: |
            go_bench_results.txt
            rust_bench_results.txt
